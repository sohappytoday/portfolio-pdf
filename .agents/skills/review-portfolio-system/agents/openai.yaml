interface:
  display_name: "Review Portfolio System"
  short_description: "Independently score portfolio quality gates"
  default_prompt: "Use $review-portfolio-system to run an independent evidence-backed acceptance review and report the lower reviewer score."
policy:
  allow_implicit_invocation: true
