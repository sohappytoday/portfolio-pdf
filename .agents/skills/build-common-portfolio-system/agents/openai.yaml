interface:
  display_name: "Build Common Portfolio System"
  short_description: "Build the reusable portfolio visual core"
  default_prompt: "Use $build-common-portfolio-system to create or refine the company-neutral portfolio system and pass its quality gate."
policy:
  allow_implicit_invocation: true
